spring:
  application:
    name: product-management-api

server:
  port: 8080

# Database Configuration Template (to be configured when database/entities are added)
# spring:
#   datasource:
#     url: jdbc:postgresql://localhost:5432/product_db
#     username: postgres
#     password: password
#     driver-class-name: org.postgresql.Driver
#   jpa:
#     hibernate:
#       ddl-auto: update
#     show-sql: true
#     properties:
#       hibernate:
#         format_sql: true
